#import "../template.typ": note

This should be in-line#footnote[There should be a footnote at the bottom of this article.]: $e^(i pi) = 1 .$

This should be in a whole line:

$ e^(i pi) = 1 . $

Let's see if the equations work:

$ nabla dot arrow(E) &= rho / epsilon_0, \
  nabla dot arrow(B) &= 0, \
  nabla times arrow(E) &= - (partial arrow(B)) / (partial t), \
  nabla times arrow(B) &= mu_0 arrow(J) + mu_0 epsilon_0 (partial arrow(E)) / (partial t). $
